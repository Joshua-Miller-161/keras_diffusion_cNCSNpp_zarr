# Array / IO stack
zarr==2.18.2
xarray
netCDF4
h5py

# PyTorch 2.x with CUDA wheels (latest stable 2.x)
# extra-index-url lets pip still use PyPI for zarr/xarray/etc.
--extra-index-url https://download.pytorch.org/whl/cu118
torch>=2,<3
torchvision
scipy